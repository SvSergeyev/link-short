spring.application.name=link-short
server.servlet.context-path=/projects/link-shortener

spring.datasource.url=jdbc:h2:file:./data/link-shortener;AUTO_SERVER=TRUE
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.h2.Driver

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
logging.level.org.hibernate.SQL=DEBUG

server.port=8084

links.baseUrl=http://sergeyev.tech/r/
#links.baseUrl=http://localhost:${server.port}${server.servlet.context-path}/redirect/
links.expirationDays=30

token.maxAttempts=10
token.length=9
